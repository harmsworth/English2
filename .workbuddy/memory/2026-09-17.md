# 2026-09-17

## 移动端设计稿生成（svgs/mobile/，纯新增，未碰 src / docs / DB）

用户要求：根据 `svgs/` 下 5 份**网页端**设计稿的样式，生成**移动端网页显示设计稿**。

**桌面稿概况**（先渲染成 PNG 逐张过目后再动手）：`svgs/{home,exam-list,exam-detail,practice,result}.svg`，
1440×960（result 1440×1016），纯矢量无位图。设计语言：
- 暖白底 `#FAF9F6`、白卡片 + `#EBE8E2` 描边、深墨绿主色 `#1F4A45`、
  状态徽章（灰 `#F0EEE9`/`#5B6663`、琥珀 `#FBF0D9`/`#9A6B12`、绿 `#E3F1EA`/`#2E7D5B`）、红 `#B04A3E`
- 衬线大标题（Georgia 系）+ 无衬线正文；顶部导航（logo+EXAM ARCHIVE 徽标+AL 头像）；表格 + 进度条 + 题号格子

**产出**（全部手写 SVG，`viewBox` 375 宽，文案与数据沿用桌面稿）：
- `svgs/mobile/home.svg`（375×1120）：导航收进底部标签栏（Home/Exams/Study/Settings，激活项顶部短横条），
  按钮全宽纵排（触达 44+），右栏「Your Performance」下移为整宽卡片
- `svgs/mobile/exam-list.svg`（375×1040）：表格 → 卡片列表（年份 + 徽章 + 进度条 + Open），
  搜索/年份筛选整宽化，尾注「Showing 6 of 17 papers」
- `svgs/mobile/exam-detail.svg`（375×1140）：Section 卡纵排（**序号圆徽即题组编号，标题去掉「Section N:」前缀**，
  否则与右侧状态徽章重叠），Instructions 右栏 → 整宽卡片，按钮纵排
- `svgs/mobile/practice.svg`（375×1800，**双画板**）：①主答题屏（右侧栏收起、题号/章节进度前移、
  sticky 底部操作条 Mark/Previous/Next Question）②Question Map 底部抽屉态（7 列题号格子 + 图例 + Submit）
- `svgs/mobile/result.svg`（375×1120）：大分数移到眉标行右上避免与标题相撞；三统计卡一行三列；
  Breakdown/错题回顾纵排；按钮全宽
- `svgs/mobile/index.html`：总览预览页（深底 + 手机壳框 + 每页重排要点注释）

**自检方法**：Playwright 把每份 SVG 截图后逐张肉眼检查，共抓出并修复 4 类问题：
1. **CSS 覆盖 fill 属性**：`text { fill: #1A2626 }` 会压过 `fill="#FFFFFF"`（表现属性优先级最低），
   所有白字/彩字被渲染成深色 → 改 `text:not([fill]) { fill: #1A2626 }`，一处修改全局生效
2. 详情页 Section 标题与右侧徽章重叠 → 标题去掉「Section N: 」前缀（序号已在圆徽里）
3. 结果页大分数与标题相撞 → 分数移至眉标行右上；Breakdown 行标签同样去前缀
4. 练习页头部标题被计时器裁切 → 标题下移到第二行，章节名并入进度条标签

**边界**：只新增 `svgs/mobile/` 目录（6 个文件），未改任何 `src` / `docs` / 线上库；Phase 7 的未提交改动保持原样。
字体用系统栈（Georgia / 系统无衬线），未引入外部字体 —— SVG 用 `<style>` 内嵌类，`<img>` 引用也生效。

## Design Direction + Design System 草案（docs-next/，**纯分析，未改代码**）

用户要求：分析 PC + Mobile 两套 SVG 设计稿，提取视觉语言/布局/Typography/颜色/Spacing/组件/响应式规则，
并与现有项目结构建立映射，输出到 `docs-next/`。产出 3 个文件：

- `docs-next/README.md`（索引 + 三句话总结 + 6 个待决策项）
- `docs-next/design-direction.md`（方向、5 条原则、视觉语言、版式哲学、字体/色彩方向、动效、a11y、现状 Gap、S1-S3 落地路径、开放问题）
- `docs-next/design-system.md`（Token 表 + 16 个组件规格 + 18 条响应式规则 + 代码映射表 + 迁移顺序）

**核心结论（后续做 UI 时直接可用）：**

- **方向**：English2 应从"默认组件搭出来的可用界面"变成"安静的真题档案馆" —— 衬线标题 + 暖白纸感 + 深墨绿单一强调色。
- **差距约 60% 是 Token 层替换**（主色 indigo→`#1F4A45`、底色纯白→`#FAF9F6`、
  `--font-heading` 从 `var(--font-sans)` 改衬线栈、描边 `ring-foreground/10`→`#EBE8E2`），
  **40% 是结构性**：缺 App Shell（顶栏/底部标签栏）、缺 Badge/Progress/Table/Sheet 组件、检索页 768 单栏→1280 + 2:1 分栏。
- **阅读区例外（重要）**：只加宽检索型页面（列表/详情/结果）；**题干/篇章/解析保持 720–768px**，
  现有 `max-w-3xl` 恰好等于该宽度，不必动。
- **中英混排**：标题走「拉丁衬线 + 中文黑体」—— CJK 回退**不要**进 serif，否则 Windows 落到宋体很难看。
- **实测发现 3 处配色不达 WCAG AA**（脚本算的，非估算）：
  `muted #6F7B77` on 暖白 = 4.18 → 改 `#6A7571`(4.54)；success on soft = 4.29 → `#2C7958`(4.53)；
  warning on soft = 4.14 → `#926511`(4.54)。`subtle #8A948F` 仅 2.97 → 限定装饰用途。
  描边对比度 1.16/1.48 < 3 ⇒ **状态必须有文字/图标冗余**。
- **移动端重排规则**：表格→卡片列表、右侧栏→下移整宽卡片或底部抽屉、顶部导航→底部标签栏、
  按钮并排→全宽纵排(44)、练习页高频操作→sticky 底部操作条。
- **设计稿领先实现的功能**（有稿无代码，别为对齐稿子临时造功能）：计时器、题号地图、Submit Examination。

**踩到的坑（写文档时）**：控制台直接输出中文会乱码（PowerShell 管道编码），
需要脚本结果落盘后再 Read，或让脚本只输出 ASCII 数字。

**边界**：本轮**只新增** `docs-next/`（3 个文件），未改 `src/`、`docs/`、线上库；
Phase 7 未提交改动与 `svgs/` 均保持原样。

## 现状代码审查（docs-next/refactor-audit.md，**纯审查，未改代码**）

用户要求：基于设计分析审查现有实现，区分「必须重构 / 建议重构 / 无需修改」，并确认应保留的业务逻辑与组件。
产出 `docs-next/refactor-audit.md`（第 4 个文件），结论：**6 必须 / 11 建议 / 14 保留 + 逐文件判定表**。

**🔴 必须重构（6）**
1. **主题 Token 重置** —— `:root` 是 shadcn 默认 indigo + 冷灰；`@theme inline` 已把 `--color-*` 指向 `:root`，
   ⇒ **只改 `:root` 值即可，不动任何组件**（成本极低）
2. **`--font-heading` 切衬线** —— 现在 `= var(--font-sans)`；接入点已有 6 处（各页 h1/年份/Card 标题），只差字族
3. **抽 `Badge`** —— 现有只有 `MistakeCard` 一处内联（`bg-destructive/10` 透明度叠色）；
   设计稿里徽章出现 4 类 6+ 处，且"透明度叠色"换主色后全要重调
4. **触控 44** —— Button 最大 `h-9`(36)；练习页上下题、错题页按钮与分类 tab（≈30）全部不达标
5. **对比度 + 状态冗余** —— 换色时必须一并修，否则把不达标色固化进 Token
6. **App Shell** —— 路由直接铺页面，每页各自「← 返回首页」；设计稿 5 页全带顶栏

**🟡 建议（11）**：Card 规格（16→12、ring→border）、容器宽度与分栏、列表表格化、详情信息结构、
练习页 sticky 操作条、结果页重排、Progress 组件、Input/Select、骨架屏去 `animate-pulse`、
图标统一 lucide（已在依赖里）、深色模式处置。

**🟢 明确保留（14，最重要的一节 —— 视觉重设计最易误伤）**
- 架构/数据层：分层链路、列白名单、**答案边界**、判分唯一出口 RPC + PracticeResult 唯一渲染处、
  排序契约、查询键、错误归一化、路由懒加载 + `:paperId` + UUID 预校验
- 业务逻辑（都踩过坑，禁止"简化"）：`canSubmit = hasChoiceItems || canRevealReference`、
  `answeredCount` 逐题判定、翻译题「离线作答+在线标记」且撤销=真删行、`onToggleTextAnswer` 未传不渲染、
  正确率按已判分客观题为分母、RPC 内错题收录
- 题库容错：json-utils 运行时窄化（不用断言）、chart 三态 + chart_url、passage/prompt 去重、
  titles 池 + letterOnly、`source_id.endsWith('-trans')` 判翻译
- 错题本域：两步查询 + 上次选择回查、**不写 updated_at**（有触发器）、不展示答案、**不做乐观更新**
- 已符合设计的实践：StateCard/FullScreen/CenteredState 文案风格、role=alert、fieldset/legend、
  reduced-motion 全局兜底、focus-visible ring

**两个易被忽略的事实（写进文档了）**
- **设计稿只有 5 页，没有错题本页与登录页** ⇒ 这两页不属于"必须重构"，套用组件规格保持一致即可，
  不要为了对齐一个不存在的稿去改结构。
- S3/S4/S6 需要"状态/进度/建议时长"等字段，而现有 `ExamPaper = id|year|title` 与查询白名单**不含**这些
  ⇒ 那是**数据契约变更**，不是样式工作，需另开议题；不要混在重设计里做。

**边界**：本轮只新增/更新 `docs-next/`（4 个文件），未改 `src/`、`docs/`、线上库。

## 设计重构落地（改代码，仅表现层）

用户拍板：**全部都改**（S1+S2 一起做）、标题走 **Georgia 系统回退**（不引入 source-serif-4）、
**深色模式冻结**。产出第 5 份文档 `docs-next/refactor-log.md`（决策 + 已落地 + 刻意未做 + 验证 + 后续）。

**6 项决策**（审计 §7 的开放问题）：Study→`/mistakes`、Settings 不放入口（底栏只 3 项）；深色冻结；
检索页 1280 / 阅读区 768；**不新增状态·进度数据字段**（表格只列真实数据，宁可缺列不造假）；
计时器/题号地图/整卷提交不做；衬线用系统 Georgia。

**改动清单**（`git status` 确认 `services/` `hooks/` `lib/` 一行未动）：
- `src/index.css`：`:root` 换暖白+墨绿色板；`@theme inline` 补 14 个扩展色映射；`--radius` 12px；
  圆角改**显式取值**（tile 7 / control 8 / card 12 / sheet 16，不再按 `--radius` 比例推，否则改 radius 会连控件一起变形）；
  `--font-heading` = Georgia 栈（CJK 用黑体回退，别写宋体）；`@layer base` 给 h1–h3 挂 `font-heading` + 负字距
- 新建 `ui/badge.tsx`（5 变体×2 尺寸，色值用达标值）、`ui/progress.tsx`、`layout/app-shell.tsx`
- `ui/button.tsx`：新增 `md`(40)/`touch`(44)；`secondary` 对齐 soft 配色；`destructive` 改 `danger-soft` 底 + danger 字（不用透明度叠色）
- `ui/card.tsx`：`rounded-lg` + `border border-border`（原 `rounded-xl` + `ring-foreground/10`）；内边距移动 16 / 桌面 20
- `router/index.tsx`：ProtectedRoute 下再包一层 `AppShell`（**不懒加载**，否则每页首帧闪无导航空白）
- 5 个页面：容器/表格/分栏/sticky 操作条/结果页 Hero+StatBlock/搜索与年份筛选/骨架去 pulse/lucide 图标

**两个自己加的判断**
- 答题页在 `/practice/` 路由**隐藏全局底部标签栏** —— 它自带 sticky 操作条，两条底栏会打架
- 错题本用 `READING_CONTAINER`(768) 而非 1280 —— 卡片里是题干+选项，1280 下长行难读

**验证**：`tsc -b` 0 / `pnpm lint` 0 error（9 warning，8 条是既有 only-export-components）/ `pnpm build` ✅；
冒烟（1440+375）**29/29**；业务回归（答题→判分→错题收录→结构级泄露扫描）**8/8**；
线上清理后读回 `mistakes/reviews/sessions/answers = 0/0/0/0`。

**踩坑**
- Playwright 断言 `getComputedStyle(body).backgroundColor` 在 Chromium 里可能回读成 **`oklch(...)` 原值**而非 rgb，两种都要接受
- `page.wait_for_url()` 触发时**旧页面可能还没卸载** —— 只 `wait_for_selector('h1')` 会命中上一页的 h1，
  必须等新页独有的元素（如练习页的「上一题」按钮）才算真正渲染完成
- Bash 工具里 `rm` 不可用（safe-delete shim 缺依赖）→ 删文件走 PowerShell `Remove-Item`

**边界**：只改表现层；`docs/` 未动（**待办**：结论应回写 `docs/pages/*.md` 与 `docs/development/decisions.md`）。

## 整卷练习 + 题号导航 + 计时 + 显示答案 + 主观题输入（改代码 + 线上 DB）

用户提了 7 条（原话要点）：①首页「开始学习」和「查看历年真题」跳到同一页，看不出区别；
②详情页每个大题下都挂「开始练习」不合理；②.1 右侧/移动端上方列出题目序号；
②.2 移动端左右滑动切题、PC 点右侧序号切题；②.3 移动+PC 都要一个设置图标决定是否直接显示答案；
②.4 练习页要计时；②.5 找不到整卷练习页，是不是漏了；②.6 翻译/作文要有大输入框并记录用户输入。

**结论：2.5 不是漏了** —— `practice_sessions` 早有 `paper_id` 与 DB CHECK（`session_type='exam'` ⇒ paper_id 必填），
判分 RPC 本来就按 session 判，所以整卷练习是**在既有数据模型上补页面**，不需要新表。

**改动清单**
- 新增 `src/pages/ExamPracticePage.tsx`（路由 `/exams/:paperId/practice`）：**拆两层** ——
  外层只负责「试卷 + 会话就绪」，内层 `ExamPracticeBody` 只在两者都就绪后挂载。
  这样计时/草稿等「挂载即起点」的状态**不需要任何 effect 同步**（见踩坑 1）。
- 新增 `components/practice/{QuestionNavigator,PracticeSettings,TimerChip}.tsx`、
  `hooks/{use-swipe,use-elapsed-seconds}.ts`、`ui/textarea.tsx`
- `HomePage`：`开始学习` → 最新一套的整卷练习（`papers[0].id`，`getCurrentExamPapers` 已 year DESC）；
  `查看历年真题` → `/exams`；顺手补了 `papers?.[0]?.id`（strict 下 `noUncheckedIndexedAccess` 报 TS2532）
- `ExamDetailPage`：移除逐大题「开始练习」，收敛为**一个**「开始整卷练习」；右侧「使用说明」文案同步改写（原本还在讲"逐大题进入练习"）
- `PracticeQuestion`：新增 `revealed`（提前揭示块 `RevealedAnswer`）、`textAnswer`/`onTextAnswerChange`（主观题受控大输入框 240px + 字数）
- `PracticeResult`：新增 `textAnswers` prop，主观题分支显示「我的作答」（判分 RPC 不返回 `text_answer`，只能从会话自己的答案回读）
- `app-shell`：练习页隐藏底部标签栏的判定由 `includes('/practice/')` 改为 `pathname.split('/').includes('practice')`
  —— 原写法**漏掉不带尾斜杠的整卷路由**
- `services/practice.ts`：新增 `peekItemAnswer()` + `RevealedItemAnswer`；`hooks/use-practice-mutations` 等未改
- `src/types/database.ts`：`pnpm db:types` 重生成（**新 RPC 必须重生成，否则 `supabase.rpc('<新名>')` 报 TS2345**）

**线上 DB 变更（用户豁免：不落 migration、不备份；执行前 git status / 执行后读回核对）**
1. `create or replace function peek_item_answer(p_session_id uuid, p_item_id uuid)` ——
   SECURITY DEFINER + 校验 `user_id = auth.uid()` + 结果 scoped 到该会话的 section/paper 范围，
   **一次只回一题**的 `correct_option / explanation / reference_translation`。收回 public、grant authenticated。
   ⇒ 答案出口从「1 个」变成**恰好 2 个**受控 RPC，常规表查询的白名单与列级 REVOKE 一行未动。
   「显示答案」放宽的是*什么时候*能看，不是*能不能批量拿*。
2. `create or replace function grade_practice_section` —— 仅改 `order by` 为 `coalesce(es.sort_order,0), si.item_no`
   （整卷会话下 `item_no` 每大题从 1 重开，只按 item_no 排会串大题）。**返回列未变 ⇒ 无需 drop/regrant。**
   两个函数读回核对：`auth_exec=true`、`has_section_order=true`、`is_scoped=true`、`total_items=816`。

**特意加的 3 处「不然就是坏手感」**（都不在用户清单里，但缺了就是半成品）
- **乐观态**：选项 / 标记 / 草稿三类本地 state，`answeredIndices = 服务端行 ∪ 乐观态`。
  否则点一下选项要等网络回读才高亮 —— 分大题练习页早有 `pendingSelection`，整卷页初版漏了（见踩坑 2）
- **卸载兜底 flush**：主观题 700ms 防抖，若只 `clearTimeout` 不 flush，"打完字立刻点返回"会丢字
- **翻译/写作不判分**：写作题沿用「不给假提交入口」的口径

**验证**：`tsc -b` 0 / `pnpm lint` **0 error（10 warning，全是既有 only-export-components）** / `pnpm build` ✅（ExamPracticePage chunk 15.98 kB）；
浏览器 smoke（1440 + 375）**21/21 全绿**，含：两个首页入口分流、详情页整卷=1/逐大题=0、题号 48 格 + 三态图例、
点击与左滑切题、齿轮开关默认关→打开出现「正确答案」、计时 00:00:00→00:00:03、翻译 240px 输入框 + 防抖写库 + **刷新后值一致**、
结果页「我的作答」回显、底部导航已隐藏、375px 无横向溢出、**常规表查询未下发任何答案列**、控制台 0 error。
线上测试数据已清理并读回（本次窗口 `0 sessions / 0 answers`，本次 `mistakes` 0 条）。

**踩坑**
1. `useElapsedSeconds(initialSeconds)` 若用 effect 做「0 → N」二次播种，会同时踩
   `react(set-state-in-effect)` 与「每 30s 写回把自己的计时重置」两个问题。
   正解是**让 hook 只在挂载时采纳初值**，把「会话就绪」变成挂载条件（拆内外两层组件）。
   ref 也不要在 render 期赋值（`react(refs)` 告警 + React Compiler 跳过优化）→ 移到 effect 里。
2. Playwright `locator.check()` 会因为「点完状态没变」直接抛
   `Clicking the checkbox did not change its state` —— 这个报错**恰好是乐观态缺失的探针**，别当成脚本问题绕过。
3. ⚠️ **`uppercase` 类的眉栏，Chromium `innerText` 返回大写**（`Full Paper Result` 读回 `FULL PAPER RESULT`），
   正则不加 `/i` 就永远匹配不上。我因此白跑一整轮 smoke 并误判"提交后没进结果视图"，
   还去翻了 DB 会话状态才排除 —— 实际耗时 **0.3s**。**断言文本一律忽略大小写。**
4. 答案泄露扫描要**排除那两个受控 RPC**（它们本来就该带答案列），否则必然误报；
   真正要守的是「常规表查询（`/rest/v1/<table>`）零命中」。
5. 本机 PowerShell 工具**只回传退出码、吞掉 stdout** ⇒ 输出必须 `Out-File` 再 Read；
   且命令不存在（`CommandNotFoundException`）会**终止管道**、`Out-File` 不执行 → 文件没生成就等于"命令没找到"。
   Bash 工具 PATH 更残（连 `mkdir`/`cat`/`head` 都没有），只有 `git` 可用。
6. `agent-browser` **未安装**。改用 `playwright-core`（装在 `C:\Users\BSI\.workbuddy\binaries\node\workspace`）
   + **显式 `executablePath` 指向缓存内核** `…\ms-playwright\chromium-1228\chrome-win64\chrome.exe`，
   绕过 playwright 版本与内核 revision 的匹配问题。

**遗留（待用户定）**
- `/exams/:paperId/practice/:sectionId` 与 `PracticePage.tsx` 现在**没有任何 UI 入口**（用户否掉了逐大题按钮）。
  要么保留为将来的「只练这一大题 / 错题重做」入口，要么删掉 —— 未擅自删。
- `docs-next/refactor-log.md` §3/§6 曾把「计时器 / 题号地图 / 提交整卷」列为刻意不做，本轮已补上，文档已同步；
  `docs/pages/*.md` 与 `docs/development/decisions.md` 仍未回写（本轮已回写，见下节）。

## 首页未完成提示 + 练习记录页 + 题库双模式 + 倒计时 + 选中后揭示答案（改代码 + 线上 DB）

用户 5 条要求：①首页要显示**未完成的练习**用于提示；②新增**练习记录页**（练习记录 + 未完成 + 图表统计，且分「整卷记录 / 题型记录」）；
③「答题时显示答案」应当是**用户选中选择题选项之后**才显示答案，**翻译和作文不显示**；④练习页把**正计时改成倒计时**（按考研英语二标准考试时长）；
⑤题库要**区分整卷练习与题型练习** —— 整卷是从头到尾做一遍，题型练习让用户选年份 + 题型，把同类题型整合到一起做。

**3 个拍板结论**（AskUserQuestion）：倒计时按题量比例折算 `ceil(items × 180 / 48)` 分钟、向上取整到分钟；
记录页入口 = **底部标签栏第 4 项 + 顶栏**；**超时自动提交并进结果页，若 0 作答则置 abandoned**（不产生空结果页）。

**关键设计判断**
- **抽 `PracticeRunner`**：整卷 / 单大题 / 题型练习三种模式页面结构 95% 相同（题号导航 + 倒计时 + 乐观态 + 揭示 + 结果视图），
  抽成一个共用答题器，各页只负责「把数据摊平成 `questions` + `groups`」并传文案。三个页面加起来省掉约 1200 行重复。
- **题型练习不新建表**：`practice_sessions` 加 `drill_type` + `drill_years` 即可表达「跨年 × 同题型」的大题集合，
  它天然放不进单个 `section_id`/`paper_id`。会话对账必须按 **year-set 相等**匹配（`.contains()` 只是子集，会错误复用别的会话）。
- **记录页统计不放开判分列**：新增两个 **counts-only** 聚合 RPC（`practice_session_stats` / `practice_type_accuracy`），
  只回次数与对错计数。若为了画图把 `is_correct` 下发前端，等于把答案边界挖开一个洞。

**改动清单**（`git status`；`services/` 之外的分层一行未动）
- 新增：`components/practice/{PracticeRunner,DrillLauncher}.tsx`、`pages/{DrillPage,RecordsPage}.tsx`、`lib/practice-record.ts`、`hooks/use-practice-clock.ts`
- 改写：`ExamPracticePage`（整卷）/ `PracticePage`（单大题）/ `DrillPage`（题型）三页均改为委托 `PracticeRunner`
- `ExamListPage`：双模式 Tab（`?tab=drill`）；`HomePage`：未完成练习块（最多 3 条 + 继续练习）
- `app-shell`：底栏 4 项（首页/题库/记录/错题本）；练习类页隐藏底栏判定 `practice | drill`
- `router`：`/drill/:sessionId`、`/records`；`constants.ts`：`examKeys.drill`、`practiceKeys.{incomplete,stats,typeAccuracy}`、`deriveTimeLimitSeconds()`

**线上 DB 变更（用户豁免：不落 migration、不备份；执行前 git status / 执行后读回核对）**
1. **修复 `practice_sessions_type_check`** —— 该表有**两个** CHECK，上一轮只放宽了 `..._target_check`，
   `..._type_check` 仍只允许 practice|exam|mistake ⇒ drill 会话 INSERT 直接 `23514`。
   已 drop + 重新添加，白名单补 `drill`。
2. **修复 `peek_item_answer`** —— 该 RPC 的 scoped 判断只覆盖了 practice（section_id）与 exam（paper_id），
   **漏了 drill 分支** ⇒ 题型练习里选完选项不显示答案（整卷/单大题正常）。用 `create or replace` 补 drill 条件：
   `ps.session_type='drill' and es.type = ps.drill_type and exists(select 1 from exam_papers p where p.id = es.paper_id and p.is_current and p.year = any(ps.drill_years))`。
   两个 bug 都是**冒烟脚本抓到的**，不是看代码看出来的。
3. 新增 `practice_session_stats()` / `practice_type_accuracy()`（SECURITY DEFINER + 校验 `auth.uid()`，counts-only）。

**验证**：`tsc -b` 0 / `oxlint` 0 error / `pnpm build` ✅（路由全部 code-split）；
浏览器 smoke **28/28 全绿**（1440 + 375）：R1 首页未完成提示、R2 记录页四指标 + 两张图 + 两个历史 Tab、
R3 选中后揭示答案且**切题后答案块消失**、翻译/写作 `peek` 调用数 = 0、R4 倒计时（题型 02:30:00 / 整卷 03:00:00）、
R5 题型练习启动与对账、**答案边界回归**（常规表查询零答案列，答案只经 3 个受控 RPC）、移动端 4 tab 无溢出、console 0 error。
线上测试数据已清理并读回 `0 sessions / 0 answers`。

**踩坑**
1. ⚠️ **`supabase db query --linked` 把整个 `.sql` 文件包成一个事务** ⇒ 我在同一文件里写 `begin; 探针; rollback;`，
   结果**把同文件的 `alter table` 一起回滚了**，读回仍是旧定义，白改一轮。**DDL 单独一个文件，探针单独一个文件。**
2. ⚠️ 冒烟里 `page.locator('button', {hasText:'退出'})` **同时命中练习页退出与顶栏登出**，
   一次误点把用户登出，后续步骤全部级联失败（表现为 console 400/401 + 页面空）。收窄为 `main button` + 加 `assertLoggedIn()` 守卫。
3. ⚠️ 三条「失败」是**断言本身写错**而非应用 bug：结果页只列**已作答**的题（所以翻译题才是唯一一行，编号是全局 46 而非 1）；
   翻译题「不显示参考译文」的断言撞上了 `PracticeQuestion` 自己那句「标记完成后可查看参考译文」提示文案；
   趋势图需要**至少 2 道已判分客观题**才画得出来，而整卷那一轮恰好一道客观题都没答。
   —— 结论：把「断言失败」当功能 bug 之前，先确认断言的前提成立。
4. 收尾清理了 smoke 造出的会话（`0 sessions | 0 answers | 2 mistakes`）。

**遗留**
- `/exams/:paperId/practice/:sectionId` 与 `PracticePage.tsx` 仍无 UI 入口（保留待用）。
- 本轮已回写 `docs/development/decisions.md`（2 条决策）与 `docs/pages/{records,drill}.md`（新增）+ 受影响页面的增量小节。

## 作答期零请求 / 退出批量落库（改代码 + 线上探针取证与清理）

用户实测报 3 条网络行为问题（URL `/drill/d6807065-…`）：①页面**静止放着也在调接口**；
②随便**点一个选项就调好几次接口**；③期望「选中后不调接口、切题也不调接口保存，
只在**点退出 / 返回上一页**时自动保存」。

**根因（三处叠加）**
1. `PracticeRunner` 有 **30s 周期回写**（把已用秒数 PATCH 进会话）⇒ 静止也持续发请求；
2. 每次选择 / 标记 / 输入**各自** `upsertAnswer.mutate` / `deleteAnswer.mutate`；
3. 这些 mutation 的 `onSuccess` 又 `invalidateQueries` 会话 + 作答两处 ⇒ **一次点击放大成 3–4 个请求**。

**改法**
- `pendingRef = { upserts: Map, deletes: Set }`：点选 / 标记 / 输入 / 切题**一律只改内存，0 请求**；
- 新增 `services/practice.ts#savePracticeAnswerDrafts({ sessionId, upserts, deletes })` ——
  **一次** `upsert(rows, { onConflict: 'session_id,item_id' })`（+ `deletes` 非空时一次 `delete().in(...)`）；
- 新增 `hooks/use-practice-mutations.ts#useSavePracticeAnswerDrafts()`，`onSuccess` 用
  `invalidateQueries(practiceKeys.all, { refetchType: 'none' })` —— **只标脏、不重取**；
- 删掉 30s 周期回写；提交时才写真实已用秒数；落库时机 = 退出 / 返回 / 提交（`flushAnswers()` 先于判分 RPC）
  / 卸载 / `pagehide` / `visibilitychange`；
- 「暂停并退出」改为**落库成功才 `onExit()`**，失败留在页面提示重试；有未落库改动时显示「退出时自动保存（共 N 处）」；
- 给 `useUpsertPracticeAnswer` / `useDeletePracticeAnswer`（单题即时写库）加 ⚠️ 注释：**答题页禁止再引入**。

**顺带修掉一个渲染 bug（回归测试发现）**
`DrillPage` 曾把 `session.status === 'completed'` 直接当结束态渲染 ⇒ 判分成功后**结果页刚渲染就被顶掉**。
改为「**只在进入时判定一次**」：`(status === 'completed' || 'abandoned') && graded === null`
（`graded` 非空 = 本页刚判的分，要继续留在结果视图）。
先用 `useEffect` + `useState` 做一次性快照，触发 `react(set-state-in-effect)` 告警；改成**纯派生**判断后告警消失。

**验证**
- `tsc -b` 0 error / `oxlint` **0 error（12 warning，全是既有 only-export-components）**；
- `probe5c` **11/11**：A1 静止 40s = 0 请求；A2 切题 = 0；B1 点选 = 0；B2 出现未保存提示；
  B3 切题 = 0；B4 退出 = **1 次** `POST practice_answers`（**2 行合并同一请求体**）+ 1 次 `PATCH practice_sessions`；
  B5 该次退出总计 3 请求；B6 重进已持久化。
- `probe6c` **5/5**：选完选项恰好 1 次 `peek_item_answer`；提交 → **先落库再判分**（`answered=2 gradeCalls=1`）；
  结果页正确渲染答案与统计。

**线上探针取证与清理（重要）**
- 用 `supabase-js` + `.env.local` 的 publishable key + dev 登录做**只读盘点**（**不走 CLI**，避开 CJK 乱码），
  发现线上有 **4 会话 / 10 作答 / 5 错题**。
- 取证结论：`770c163a-…`（exam）的作答里写着 `"SMOKE4 typed translation."` —— **是我早期 smoke4 造的**，
  而此前几轮的清理脚本**误把它当成「用户会话」保留了**；`smoke4d/e` 输出也印证（`items=40` = 完形填空 [2024,2025]、
  `48 items` = 2026 整套）。
- 最早 15:07 的快照里，14:18:18 / 14:19:35 / 14:19:39 / 14:19:49 **91 秒内冒出 4 条 `practice` 会话**
  ⇒ 人工不可能，全是自动化产物；据此判定 14:16 / 14:18 那两条错题也是探针残留（来源会话早已被清掉）。
- 清理：删 3 条探针会话（`bb53a06f` / `7e851389` / `770c163a`）+ 5 条探针错题；
  **保留** `d6807065-…` —— 它虽然最初也是 smoke 造的，但用户后来**在上面做了自己的作答**（16:53 / 16:54），
  且报障 URL 就是它。最终读回：**1 会话 / 3 作答 / 0 错题 / 0 复习**。

**踩坑**
1. `exam_sections` **没有 `year` 列**（年份在 `exam_papers`）；且 `select('*')` 会因列级 REVOKE 直接
   `42501 permission denied` ⇒ 取证脚本必须用 `src/services/exams.ts` 里的白名单列名。
2. `Test-NetConnection 127.0.0.1:5173` 报 False，但 vite **其实在跑** —— 它绑的是 **IPv6 `[::1]`**；
   `netstat -ano | Select-String :5173` 才是真相（`localhost` 可达而 `127.0.0.1` 不通）。
3. 沙箱里 **`supabase` CLI 既不在 node_modules 也不在 PATH**（此前用过的 `npx supabase` 路径已不可复现）
   ⇒ 需要读写线上库时，改用 `supabase-js` + 用户登录（RLS 限本人）这条更稳的路。
4. ⚠️ **同一文件并行发多个 Edit 会互相覆盖**：一次发「删 `useEffect` 导入」+「删 state 块」两个 Edit，
   结果导入那条没生效（`useEffect` 还在，`tsc` 报 TS6133）⇒ 改完必须 Read 复核，别只看 "Successfully edited"。
5. `Start-Process -WindowStyle Hidden` 起 vite 会**静默退出**（stderr 为空）⇒ 用后台任务方式起，或直接复用已在跑的实例。

**回写文档**
`decisions.md`（新增「作答过程零网络请求：只改内存，退出 / 提交时批量落库」）、
`pages/practice.md`（新增 §6「保存时机」+ 改写状态表 / 操作表 / 数据来源 / 判分通路 / 验收标准 / 当前状态）、
`pages/drill.md`（结束态判定 + 零请求语义 + 验收）、`development/progress.md`（增量 2）、
`hooks/use-practice-mutations.ts`（两个遗留 hook 加 ⚠️ 注释）。

---

## 增量 3：试卷详情页补「返回题库」入口（ExamDetailPage）

**用户诉求**
`/exams/953f8188-…` 页面上要有个返回题库的入口 —— 顶栏虽有「题库」菜单能点到，
但试卷详情页本身也应该有「返回上一页 / 题库」的按钮。

**现状核查**
读 `ExamDetailPage.tsx`：页面**没有任何页面级返回入口**，只有 AppShell 的顶栏 + 移动端底栏。
而 `docs/pages/exam-detail.md` §4/§6/§10 里**早就写了** `BackLink` / `← 历年真题` —— 属**文档与实现漂移**（当时实现被丢掉了）。

**改动（`src/pages/ExamDetailPage.tsx`）**
1. 新增页内局部组件 `BackLink`：`<Link to="/exams">` + `ArrowLeft` 图标（`strokeWidth=1.5`、`size-4`、`aria-hidden`），文案「返回题库」。
2. 渲染位置：loading（骨架）态 + success 态**各一**，都在**标题上方**，用 `mt-6` 与正文拉开间距。
3. 原 404 / error 态里那个 `Link` 的文案「返回历年真题」→ **「返回题库」**（与顶栏「题库」用词统一）。

**为什么这样做**
- 需要页内入口：本页是**深链页面**（URL 带 `paperId`，可能从列表 / 练习页返回 / 直接粘贴进入），
  看完一套卷子想回列表是最高频动线，而走顶栏要求视线离开内容区再辨认导航层级。
- 用**文字链**而不是按钮：本页已有唯一主 CTA「开始整卷练习」，再造一个同级按钮会争视觉权重。
- **硬编码 `to="/exams"`** 而非 `navigate(-1)`：直接访问 / 刷新时历史栈里可能没有上一页；
  `/exams` 默认停在「整卷」页签，与顶栏「题库」一致，所以文案叫「返回题库」而非「返回上一页」。

**验证（Playwright `probe7.js`）14/14 PASS**
- A1–A6：返回题库入口存在 / `href='/exams'` / 含 svg 左箭头 / 顶栏「题库」仍在（count=1，未被替换）/
  主 CTA「开始整卷练习」`href` 未被改动 / 返回链 `y=121` 在 `h1`（`y=197`）**上方**。
- B1：点击后落到 `/exams`。
- C1：404 态同样给出「返回题库」。
- D1–D4：375px 下可见、在视口内（x=20 w=76 h=20）、无横向溢出（0px）、底栏仍在。
- E1：**渲染本页零写请求**（纯只读）；E2：console 0 error。

**踩坑（新）**
- ⚠️ 重跑 probe7 时先报 `MODULE_NOT_FOUND: playwright-core` —— 我把 `NODE_PATH` 指到了**项目的** `node_modules`，
  但 `playwright-core` 实际装在 **`C:\Users\BSI\.workbuddy\binaries\node\workspace\node_modules`**（托管 node 工作区），
  项目里**没有**这个包。指对之后立刻 14/14。

**回写文档（本轮）**
- `docs/pages/exam-detail.md`：§3 / §4 / §5 / §6 / §9 / §10 / §11 / §12 / §13 全量同步 ——
  文案统一为「返回题库」，补「返回入口为什么放在页内」的设计说明，
  验收标准新增「有且仅有一个返回入口、`href==='/exams'`、在标题上方、四态都可用、本页零写请求」，
  并在 §13 记一句「原文档写过的 BackLink 曾在实现中缺失，本轮补齐」。

**记忆维护**
- `MEMORY.md` **整体重写压缩**（原文件在注入时被截断）：确立「只记文档里没有的约定与坑」的原则，
  规格类内容全部让位给 `docs/`，保留 数据/查询坑、答案边界、DB 高危坑、作答保存契约、UI 契约（文档未覆盖部分）、Windows 环境坑位、纪律。
