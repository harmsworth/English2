# 2026-09-15

## English2 考研英语二真题学习系统 — Phase 1 项目初始化

- 发现工作区此前只有 `.git` + `data/`，前端项目并未落盘（git 仅 2 次提交）。本次从零在仓库根目录完成 Phase 1 脚手架。
- 技术栈落地版本：Vite 8.3.0 / React 19.3 / TypeScript 6.0.3 / Tailwind CSS 4.3.3 / shadcn CLI 4.21（preset `nova`）/ Base UI 1.8 / oxlint（模板已弃用 ESLint）。
- 关键坑位（务必记住）：
  1. shadcn init 报 `Could not load the workspace config` → 根因是 Vite 模板的 `tsconfig.json` 是 solution style（`files: []` + references），必须在**根 tsconfig.json** 里也补上 `baseUrl` + `paths`，否则 shadcn CLI 解析不到别名。
  2. TypeScript 6 已废弃 `baseUrl`，`tsconfig.app.json` 里不能写 `baseUrl`（报 TS5101），只写 `paths` 即可（TS 4.4+ 支持无 baseUrl 的 paths）。
  3. 新版 shadcn 生成的组件默认 `import { cn } from "cn"`，本项目规范改用 `@/lib/utils`（clsx + tailwind-merge），每次 `shadcn add` 后需改这一行。
- 项目约定：`data/exams/` 是 GitHub 源题库（Source of Truth），不可移动/重命名；`EXAM_FILES` 与 `GITHUB_RAW_BASE` 集中在 `src/lib/constants.ts`。
- 主题：白底深字 + 淡灰边框 + 克制的 indigo 主色（oklch 265 色相），字体 Geist Variable + 中文系统字体兜底，已加 `prefers-reduced-motion` 处理。
- 验证结果：`pnpm build` exit 0；`pnpm lint` 0 error / 1 warning（shadcn 自带 button.tsx 的 fast-refresh warning，可接受）；`pnpm dev` 返回 200。
- 本 Phase 未提交 git，等待用户确认后再 commit。

## 接入 @shadcn/lint（Oxlint jsPlugins）

- 安装：`pnpm add -D @shadcn/lint`（0.1.0，连带 +75 个包）。未引入 ESLint，未替换 Oxlint，未删除任何依赖。
- 版本兼容性：要求 Node ≥ 20.19（当前 v22.22.2 ✓）、Oxlint ≥ 1.80（当前 1.83.0 ✓）。
- 只改了 `.oxlintrc.json` 一行：新增 `"jsPlugins": ["@shadcn/lint"]`。`plugins`、`rules`（react/rules-of-hooks、react/only-export-components）、`$schema` 全部保留；未启用任何 `shadcn/*` 规则；未加 `settings`（components.json 已存在，自动发现 `src/components/ui` 与 `src/index.css` 主题）。
- 验证 jsPlugins 是否真被解析的方法：临时写一份 `jsPlugins: ["@shadcn/lint-xxx"]` 的配置，Oxlint 会直接报 `Failed to load JS plugin` 并以 exit 1 失败；换成真实包名则正常，说明注册生效。
- 注意：how-it-works 文档提到 linter 会优先用项目里的 `cn`（≥0.2.6），本项目已移除 `cn` 包、改用 `@/lib/utils` 的 clsx+twMerge，因此走 bundled grammar 兜底。若将来启用 `no-unknown-classes` 等规则需留意。
- 结果：`pnpm lint` exit 0（1 warning，为改动前已存在的 button.tsx fast-refresh warning）、`pnpm build` exit 0。改动文件仅 `.oxlintrc.json` / `package.json` / `pnpm-lock.yaml`。

## `cn` npm 依赖去留调查（结论：保留，不移除）

- 代码引用：全项目 `from "cn"` / `require("cn")` = **0** 处（唯一命中是 README 第 183 行说明文字）。UI 组件 button.tsx / card.tsx 均用 `@/lib/utils` 的自实现 `cn()`（clsx + twMerge）。components.json 无 cn 配置。
- **@shadcn/lint 不强制依赖 npm `cn`**（源码实证 `node_modules/@shadcn/lint/dist/index.js`）：`resolveCnConfig()` 先 `require.resolve("cn/config")`，版本 < 0.2.6 才 warn 并回退；**解析失败（catch）直接用 `bundled()`**（内置 cn 0.2.6 grammar）。且 `@shadcn/lint` 自身硬依赖 `cn: "0.2.6"`（精确锁），内置 grammar 永远可用。
- **但 `cn` 是别人的传递依赖**：`shadcn@4.21.0` → `cn@^0.2.4`；`@shadcn/lint@0.1.0` → `cn@0.2.6`。所以 `pnpm remove cn` 只会删掉根 `node_modules/cn` 符号链接 + package.json 一行，`.pnpm/cn@0.2.6` 仍在，cn 不会真正离开项目。
- **shadcn CLI 会自动写回 cn**：其 dist 中新组件固定生成 `moduleSpecifier: "cn"`（`import { cn } from "cn"`）+ "Installing dependencies" 步骤补装。实测：Phase 1 里 `pnpm remove cn` 之后跑一次 `shadcn add card`，cn 就被写回 package.json。以后每加组件都会重现。
- 另一代价：移除后 @shadcn/lint 的 class 分组从"项目 cn 0.3.0"回退到"内置 cn 0.2.6"（当前 0 条 shadcn 规则，暂无影响）。注意本项目运行时实际是 `twMerge(clsx(...))`（tailwind-merge 3.7.0），并非 cn 包，所以两条路都不是 100% 匹配。
- 决策：按用户设定的删除门槛，条件 E「其他依赖没有依赖 npm cn」不成立 → 先建议保留；**用户随后明确要求删除，已执行 `pnpm remove cn`**。
- 删除后实测（2026-09-15 10:31）：`pnpm lint` exit 0（仍是原来那 1 条 button.tsx fast-refresh warning）、`pnpm build` exit 0，产物 hash 与删除前完全一致。
- 删除后状态：根 `node_modules/cn` 消失；`require.resolve('cn/config')` 解析失败 → @shadcn/lint 走 `catch → bundled()`（内置 cn 0.2.6 grammar）；`@shadcn/lint` 自身嵌套的 `cn@0.2.6` 仍在，插件 `require('@shadcn/lint')` 正常加载（exports: plugin/project/default）。`pnpm why cn` 显示仅剩 shadcn 与 @shadcn/lint 两条传递依赖。
- **后续维护注意**：每次执行 `pnpm dlx shadcn@latest add <组件>` 后，`cn` 会被 CLI 自动写回 package.json（CLI 生成 `import { cn } from "cn"` + 自动补装），需再删一次并把新组件的 import 改成 `@/lib/utils`。
