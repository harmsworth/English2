# 2026-09-16

## Phase 5A.5 — 数据库基础设施整理 / Migration Bootstrap / 权限收口（已完成并推送，见当日提交）

> 注：本节活动实际发生在 2026-09-16（migration 文件名与 supabase/.temp 时间戳均为 09-16）；先前误记在 `2026-09-15.md` 末尾，此处为当日正式记录。

- 基线 `4a8b47c`。建 `supabase/migrations/` 5 文件（+918）：baseline schema / baseline grants / 答案边界列级 / mistakes DML / sync_exam_paper 函数，逐对象取自线上 introspection（`pg_get_functiondef`、`information_schema`、`pg_policies`、`role_table_grants`）。
- 线上（经授权）执行两处最小 DDL：① `GRANT SELECT/INSERT/UPDATE/DELETE ON mistakes, mistake_reviews TO authenticated`（P1-2 修复，RLS 未放宽）；② `section_items`/`exam_sections` 改列级 SELECT，移出 `correct_option/explanation/section_items.extra_data(reference_translation)/exam_sections.source_data`（P1-1 收口）。**未用视图**（视图牵动 exams.ts 跨表内嵌+zh 别名，风险大）。
- 验证：mistakes/reviews own CRUD + 伪造 foreign user_id 被 42501 拒 + own-only + 清理（17/17）；答案列直读被拒；回归 7/7（17 套内嵌仍 200、Practice、console/pageerror=0）。
- commit **`9383666 chore: bootstrap database migrations and permissions`** → HTTPS push → HEAD==origin/main==9383666。
- 诚实限制：Docker daemon 当时未运行 → `supabase db pull/diff` 的 clean-room 漂移校验未执行；baseline 为逐项 introspection 核对。

## 项目级全量审查（parse5c-check，只读）

- 全仓 `src` + 配置 + 线上 DB 元数据 + 真实 authenticated 只读探测 + 浏览器扫描。
- 三大发现：① 答案隔离仅在应用层（当时 exam 表 authenticated 有表级 SELECT、RLS qual=true 不限列，可 REST 直读 correct_option/explanation/source_data/翻译 passage_zh，实测 200）；② mistakes/mistake_reviews 有 RLS 无 DML 授权；③ 历史 DDL（4E/5A）未进 migrations。→ 促成 Phase 5A.5 修复。
- 本轮**未改任何文件/DB、未 commit**。

## Phase 5c-0 — 完整 SQL 备份 + passage_zh 安全收口（**BLOCKED，未执行改动**）

- 目标：A 删 `supabase/migrations/` 改「线上→一个完整 SQL 快照」；B 彻底收口 `exam_sections.passage_zh`（普通 authenticated REST 不可读）。
- 基线核对：HEAD==origin/main==9383666，src 干净。
- **④ 审计 passage_zh**：`information_schema.column_privileges` 确认 `authenticated` 目前仍持 `exam_sections.passage_zh` 列权限（5A.5 有意保留，因非翻译题「中文参考」需要）；翻译题 passage_zh = 参考译文。
- **⑤ src 依赖检查（关键）**：`grep passage_zh src` → `src/services/exams.ts` 的 `PAPER_DETAIL_SELECT` 仍内嵌 `zh:exam_sections(id, passage_zh)`（+ `.neq('zh.type','翻译')`），`ExamSection.tsx` 用它渲染「中文参考（辅助阅读）」。⇒ **直接 `REVOKE SELECT (passage_zh)` 会让全部 17 套 ExamDetail 查询 42501 报错**（不只是掉折叠功能）。要收口必须先移除前端这条读取路径（改 src）。
- **③ 备份失败（BLOCKED 根因）**：`npx supabase db dump --linked`（走 Management API 认证 + 本地 `supabase/postgres` 容器跑 pg_dump，连远端 `db.<ref>.supabase.co:5432`）反复失败：首轮 `pg_dump: SSL SYSCALL error: EOF detected`（查 pg_type），后几轮在「Dumping schemas…」静默中止、落盘 **0 字节**（多次重试，镜像已缓存 1.7GB 仍失败）。判断为**本沙箱网络到 DB 主机的长连接被中断/代理限制**；`db query --linked` 能过是因为它走 api.supabase.co 而非直连 DB。`--db-url` 直连需要 Postgres 数据库密码（不持有、不伪造）。
- **按 §四/§12/§17/§20 与 AGENTS 安全优先原则：备份是「任何线上改动之前」的强制前置**，备份拿不到就**不执行 passage_zh 的线上 REVOKE、不改 src、不删 migrations、不 commit 空备份**。仓库保持 9383666 原样。
- **待用户决策的两条收口路径**（选定 + 提供可用备份后再做）：
  1. **最简（推荐）**：`src/services/exams.ts` 去掉 `zh`/`passage_zh` 内嵌 + `RawZhRow`/`zhById`（`passage_zh` 恒 null，`ExamSection` 的「中文参考」折叠自然消失），再 `REVOKE SELECT (passage_zh) ON exam_sections FROM authenticated`；未来要展示中文参考/译文改走 5C 的 SECURITY DEFINER RPC。属 §1 允许且需先报告的「不可避免的最小 src 改动」。
  2. **保全功能**：建 `security_invoker` 视图把翻译题 passage_zh 置 null，题面读取切到视图（需处理 PostgREST 关系内嵌，改动更大）。
- **解 BLOCKED 需要**：在能稳定连 DB 的环境执行一次 `npx supabase db dump --linked -f supabase/backup/english2_full_backup.sql`（或在 Supabase 控制台生成备份并放入仓库），或提供可直连的 DB 凭据。备份就绪后再执行上面选定方案 + 重删 migrations + commit `chore: simplify database backup`。
- **本轮结束：BLOCKED，未进入任何改动，未进 5C。**

## 文档体系建设（未改业务代码 / 未 commit / 未 push）

- 只读分析了既有 `docs/00-product-requirements.md`、`docs/architecture.md`、`docs/AGENTS.md`（三份均已完整，未重写）与全部 `src/`、`supabase/migrations/`、`data/exams/`。
- 新建 4 个目录、21 个文件：
  - `docs/phases/`（README + 7 个 phase）：按用户价值划分，Phase 1–5 = done，Phase 6 = planned（下一步），Phase 7 = planned。完整考试模式 / 统计 / PWA 列为未排期，不建独立文档。
  - `docs/pages/`（README + home/login/exams/exam-detail/practice/not-found）。
  - `docs/development/`（README + workflow/progress/acceptance/decisions）。
  - `docs/data/README.md`（runtime vs offline、答案边界、快速判断表）。
- `docs/architecture.md` §37 文档树做了**一处最小对齐**（改成真实结构 + 加 `data/`），未改设计意图。
- `progress.md` 记录：Current Phase = 无（1–5 done）、Blocked = SQL 备份、Known Issues 7 条（含「开始学习」按钮无行为、`/exams/:id` 与 `:paperId` 文档不一致等）。
- `decisions.md` 记录 5 条已发生决策（passage_zh 收口、列级权限不建 View、migrations 基线、index.json 不作 Paper List、Phase 5 不判分）+ 1 条待决策（判分通路 A/B/C，阻塞 Phase 6）。
- Phase 划分依据 `00-product-requirements.md` §36 的 8 个演进方向，未自创产品方向。
- 未修改 `src/`、未碰数据库、未建 migration、未 commit、未 push；`git status` 中用户原有改动（`M src/pages/ExamListPage.tsx`、`D docs/{database-schema,parse1,parse2}.md` 等）均保持原样。
- 同步整理：`.workbuddy/memory/MEMORY.md` 已重写压缩（移除已废弃的 `zh`/`passage_zh` 双别名方案与「仓库无 migrations」等过时条目）。

## 审阅两份主文档后的 4 项修正（用户已拍板，全部执行 + 验证通过）

1. **统一 Phase 体系**（P0，比路由问题更严重）：`.codebuddy/rules/phase-discipline.md` 与 `CODEBUDDY.md` 原各有一份「10 阶段」清单，与 `docs/phases/` 的 7 阶段**编号相同含义不同**（旧 P4=题库同步 vs 新 P2=内容体系）→ 两份均改为「Phase 唯一定义源 = `docs/phases/`，本文件只留跨阶段纪律」，旧清单标注废弃。
2. **路由参数统一为 `:paperId`**：`00-product-requirements.md`（5 处）、`AGENTS.md`（2 处）、`architecture.md`（4 处）全部 `/exams/:id` → `/exams/:paperId`。**代码不动**（router 注释说明用 id 是为多版本共存）。
3. **首页 CTA**：`HomePage.tsx` 给「开始学习」加 `onClick → /exams`，删除「项目初始化成功。」占位文案（唯一业务代码改动，diff 仅 +7/-3）。⚠️ 两个 CTA 现同指 `/exams`，已记入 Known Issues，待 Phase 7 学习记录就绪后「开始学习」可改接「继续上次练习」。
4. **architecture.md 5 处过时事实对齐**：§4.2（DTO 只有 id/year/title，`is_current` 仅作过滤）、§15（补 LoginPage/NotFoundPage）、§23（补 `/login` 与 `*`，并说明 ProtectedRoute 分组）、§33（学习数据表改真实表名，删掉臆造的 `favorites`）、§34（四表已建表，不确定的只是产品用法）。
   - ⚠️ 教训：在 `docs/` 根目录的 `architecture.md` 里写相对链接要用 `./phases/...`，**不能**用 `../phases/...`（我第一次写错，已修正）。

验证：`pnpm lint` 0 errors（7 warnings 均为 router/button 既有项）、`pnpm build` exit 0（`tsc -b` 通过）；Playwright 浏览器验证 7/7（登录后首页、占位文案已移除、CTA 唯一且跳 `/exams`、列表 li=17、375px 无横向溢出、console error=0、page error=0）。
未 commit、未 push。dev server 已按 5199 端口关闭。

## 解除 SQL 备份 BLOCKED + Phase 6 判分通路方案分析

- 用户决定：**不再把 SQL 备份作为线上数据库变更的前置条件**。`progress.md` 的 Blocked 段已改写为「已解除」，`phase-06` 的第 3 条前置同步改为「变更写成新 migration 保持可追溯」。
- 就判分通路给出对比与推荐（**尚未拍板，未写 decisions.md、未写 migration**）：
  - **推荐 A：服务端判分 RPC（SECURITY DEFINER）**，且做成「提交整个大题时批量判分一次」而非每题判分。
  - **B（放宽列级权限）否决**：HTTP 无状态，数据库不知道"是否已提交"，GRANT 回来后任意 authenticated 可随时拖走全部 816 题答案；且列级权限无法用 RLS 表达（RLS 只管行不管列）。等于回退 Phase 2/5C。
  - **C（Edge Function）**：效果同 A，但要多部署一套 Deno 运行时，对家庭项目过度。
- A 的三个实现要点（将来真做时必看）：
  1. **必须在函数内校验 `ps.user_id = auth.uid()`** —— 否则任何人传任意 session_id 就能提权拿答案，这是 SECURITY DEFINER 最经典的漏洞。
  2. 必须 `set search_path = public, pg_temp`。
  3. 判分结果 `is_correct` 应在服务端写回 `practice_answers`，不要前端回写（否则错题本数据可被用户篡改）。
- 概念澄清（避免以后走偏）：安全目标**不是**"用户永远看不到答案"——提交后看到正确答案与解析正是学习闭环的核心；目标是**不能在未作答时批量获取全库答案**。
- 翻译题参考答案在 `section_items.extra_data.reference_translation`，需走同一 RPC 通路返回（该列已被 REVOKE）。

## Phase 6 Goal 6.0 完成（仅决策，未碰数据库/代码）

- 用户两点长期豁免（已写入项目 `MEMORY.md`，并在 `architecture.md` §35 标注例外）：
  **不写 migration**、**不做 SQL 备份**。代价是变更在仓库不可追溯/不可回滚，用户知情接受。
- **Goal 6.0 已定案 = 方案 A：SECURITY DEFINER 判分 RPC，提交整个大题时批量判分一次**（非逐题）。
  否决 B（HTTP 无状态 → GRANT 后 816 题可被任意拖走，且 RLS 只管行不管列无法补救）、否决 C（多一套 Deno 运行时）。
- 落文档：`decisions.md` 新增正式决策条目（含否决理由 + 六条实施硬约束）；`phase-06` 状态改 `in-progress`
  且 Goal 6.0 改写为「已完成 + 约束摘要」；`progress.md` 的 Current Phase 改为 Phase 6、新增 Completed Goals 表。
- **本轮未创建任何数据库对象、未改 src/、未 commit、未 push。**
- 下一步 Goal 6.1（客观题判分）待执行；注意第 1 步会在**线上库**建 RPC 且不在仓库留痕。

## Phase 6 Goal 6.1 / 6.2 落地 + 全量验证（**未 commit、未 push**）

用户先删除了 `supabase/migrations/`（确认放弃 migration 工作流），随后「继续」推进 Goal 6.1。

**产物（均为已修改未提交）：**

- 线上库新建 `public.grade_practice_section(p_session_id uuid)` —— SECURITY DEFINER、`set search_path = public, pg_temp`、
  先校验 `ps.user_id = auth.uid()`、批量写 `practice_answers.is_correct`、会话置 `completed`、
  只 `return query` **本次实际作答过**的题（`left`/`inner` join 到 `practice_answers`）。已 `revoke from public` + `grant to authenticated`。
- `src/services/practice.ts` +`PracticeGradeResult` / `gradePracticeSection()`
- `src/hooks/use-practice-mutations.ts` +`useGradePracticeSection()`（invalidate session/answers）
- `src/components/practice/PracticeResult.tsx`（新）—— 纯展示：总览（已作答 / 答对 / 答错 / 正确率）+ 逐题「你的选择 / 正确答案 / 解析 / 参考译文」
- `src/types/database.ts` Functions +`grade_practice_section`
- `src/pages/PracticePage.tsx` —— 提交按钮 + 结果视图 + 「正在判分…」态

**顺手修掉的两个真问题（自查发现，非用户提出）：**

1. 原草稿里有个「已完成会话进页自动取结果」的 effect —— **死代码**。
   `getResumablePracticeSession` 只查 `active | paused`，`completed` 永远不会被返回。已删并改注释说明真实行为。
2. 该死代码还带出一个真 bug：提交后若 resumable 因窗口重新聚焦刷新为 `null`，创建会话 effect 会**凭空多建一条新会话**。
   已给 effect 加 `graded` 守卫。

**踩坑：`42P13 cannot change return type of existing function`。** 第一版函数用 `CREATE OR REPLACE` 写好后又想加返回列，
被拒。**改返回列只能 `drop function` + `create function`**；而 DROP 会连带丢掉 EXECUTE 授权，**必须重新 GRANT**。

**验证（全绿）：**

- `tsc -b` 无输出；`pnpm lint` 7 warnings / 0 errors（全是既有的 `only-export-components`：button ×1 + router ×6）；`pnpm build` 174 modules OK。
- Playwright 交互轮 **16/16**：作答 3/5 → 提交 → 「已作答 3 / 5 题」+「答对 1 题 · 答错 2 题 · 正确率 33%」（正确率与 `round(1/3*100)` 一致）
  +「正确答案：」3 行 +「解析」3 块 + 未作答文案 + 作答阶段零泄漏 + 375px 无横向溢出 + console error / pageerror 均 0。
- 浏览器交叉核对判分真值：第 1 题选 A「Document their valuable traditions」→ 正确答案 D「Look into their current situation」，
  解析正文与之一致；第 3 题选 A = 正确答案 A 标「答对」⇒ 判分不是瞎标。
- **冷缓存结构级边界核验 10/10**（全新 context 直入练习页，把 `exam_papers` 响应 `json.loads()` 后逐层比对键集合）：
  `paper == {all,id,title,year}`（3 键不多不少）；9/9 section、48/48 item、195/195 option 键集合**恰等于白名单**；
  全层级递归 walk 禁止字段 **0 命中**。规模对照 9 sections / 48 items / 195 options。
  ⇒ **Phase 2 答案边界未被破坏**，判分 RPC 是答案唯一出口。
  （教训：第一轮用子串扫描误报 `score`，因为 `exam_sections.score` 是合法公开列 —— 子串法不可靠，要结构级比对。）
  另一教训：第一轮作答阶段的泄漏扫描**没覆盖** `exam_papers` 载荷，因为练习页复用了 React Query 缓存、没再发请求 —— 必须冷启动才扫得全。
- 测试数据已清理，读回线上 `remaining_sessions = 0, remaining_answers = 0`。

**文档同步：** `docs/pages/practice.md` 重写（答题/结果双视图、判分通路、已知限制）；
`docs/development/progress.md` 重写（合并了原本重复的两个 `Next Goal` 段）；
`docs/phases/phase-06-answer-review.md` 标 6.1/6.2/6.4 完成。
顺手修正 `practice.md` 里错写的「第 21 小题」—— 实测 `item_no` 是**大题内 1..N**（17 年逐一核对），不是全局题号。

**挂起项（需用户决策）：Goal 6.3 翻译题参考答案。** 它与 Phase 6 §4「不包含主观题在线输入提交」直接冲突：
翻译题永远产生不了 `practice_answers` 行 ⇒ RPC 永远不返回它 ⇒ 参考译文区块不可达。
RPC 与组件其实都已支持，只缺触达路径。选项：(a) 改写 6.3 归到后续 Phase；(b) 给主观题加独立入口（扩张本 Phase 范围）。

## Goal 6.3 方案评估（用户自己在 PracticeQuestion.tsx 里试了方案 b）

用户改了 `src/components/practice/PracticeQuestion.tsx`：text 分支加 `isTextAnswered` / `onToggleTextAnswer`
两个可选 props + 一个「标记已完成作答」按钮，意图是"标记线下做完 → 落一行 practice_answers → 提交后 RPC 下发参考译文"。

**评估结论：方向对（且范围是干净的，不算扩张 §4），但当前单独不足，现在是死按钮。**

先记一个反直觉的收获：我的方案 (b) 描述写成"扩张 Phase 6 范围"其实**过于悲观** ——
用户的设计不是"主观题在线输入提交"（§4 排除的是这个），而是"自我标记线下已完成"，
所以 **§4 不用改，6.3 可以就在 Phase 6 收口**。

**数据层已经就绪，不用改 schema、不用改 RPC 主体：**
- `practice_answers.text_answer text` 已存在可空；约束里**没有**"selected_option / text_answer 二选一"的 CHECK ⇒ 纯文本行合法
- `upsertPracticeAnswer()` 已支持 `textAnswer` 参数（practice.ts L426 / L443）
- 判分 RPC 对 text 行已正确：`item_type='choice'` 过滤 ⇒ 不判分、`is_correct` 留 null，并返回 `reference_translation`
- `PracticeResult` 对 `is_correct === null` 已按主观题渲染；`objective.length === 0` 时已不显示正确率

**两个硬阻塞（都在 PracticePage.tsx，用户没改这个文件）：**
1. **没接线** —— L288 仍只传 `item/item/selectedOption/onSelect` ⇒ `onToggleTextAnswer === undefined`
   ⇒ `onClick={undefined}` 点击无反应；`isTextAnswered` 恒 undefined ⇒ 永远显示"标记已完成作答"。
   比原来的纯说明**更糟**（看着能点其实点不动，还承诺了"参考译文与解析"）。
2. **接线了也提交不了** —— L155 `answeredCount = savedByItem.size`，而 savedByItem（L100-106）只收
   `selectedOption !== null` 的记录；翻译题写 `text_answer`、`selected_option` 恒 null ⇒ 计数恒 0
   ⇒ L319 `disabled` 永远 true ⇒ 翻译大题（仅 1 小题）**永远按不动提交**。

**四个次级缺口：**
3. RPC 返回列没有 `pa.text_answer` ⇒ 看不到"我的译文"，没法对照（要改就得 drop+create+re-grant）
4. 文案说"参考译文与解析"，但 17/17 翻译题 `explanation` 全为 NULL ⇒ 解析永不出现
5. `item_type !== 'choice'` 同时命中写作 ⇒ 写作大题点"标记完成→提交"**什么都不会显示**
   （范文只在 2024/2025 的 4 个大题，且存在公开可读的 `exam_sections.extra_data`，其余 13 年无范文）
6. service **没有 delete 函数** ⇒ "撤销"撤不掉 DB 行，撤销后仍算已作答、仍能看到参考译文

**最小修法**：PracticePage 传 props + `answeredCount` 改算"选择 + 文本" + 文案按 `source_id.endsWith('-trans')` 分派
（前端只能这么判断有无参考内容 —— 答案是列被 REVOKE 了，前端看不到）。
已把上述数据事实写进 `MEMORY.md`。**本轮未改任何文件**（除 memory）。

## 用户提交 + 推送，并对本地改动做实证复核

用户已把工作提交为 **`9621c4e 提交`**（45 files, +8654/−11026）并 **push** ⇒ 现在 `HEAD == origin/main == 9621c4e`。
该提交包含：我做的 Phase 6 代码与全套 docs、用户自己的 `ExamListPage.tsx` PaperRow 重构、
删除的 6 个 migration、以及 memory 快照。

**提交之后仍有 7 个文件未提交**（另 2 个是 memory，属我写入）：

| 文件 | 改动 | 对 6.3 的作用 |
| --- | --- | --- |
| `PracticeQuestion.tsx` | text 分支加 `isTextAnswered` / `onToggleTextAnswer`（可选 props）+「标记已完成作答」按钮 | 必要但不充分 |
| `src/services/practice.ts` | DTO 映射加 `?? null`（`row.is_correct ?? null` 等 5 处） | 正确性修复，不解决可达性 |
| `src/types/database.ts` | 重新 codegen，去掉我手写的注释与 `\| null` | 类型纯净度，不解决可达性 |
| `package.json` | 新增 `db:types` 脚本 | 工具链 |
| `README.md` / `docs/architecture.md` / `docs/phases/README.md` | 文档同步；README 的 Roadmap 里**明确写着「主观题（翻译 / 写作）在线作答」尚未实现** | 文档 |

用户对 `database.ts` 的处置是对的、而且比我的做法更干净：**codegen 产物不手写**，
codegen 对 `RETURNS TABLE` 一律推非空 → 由 `practice.ts` 的 DTO 映射层用 `?? null` 兜住。
（反过来我原先手写 `| null` 进 `database.ts` 是不该做的，已改记进 `MEMORY.md`。）

**实证复核（不是推演）**：`tsc -b` / `pnpm lint` / `pnpm build` 全 exit 0（用户的改动编译干净）。
再用 Playwright 进 2026 翻译大题（`/practice/5afc3883-…`，第 7 大题，第 1/1 题）实测，**9 条断言只有 5 条通过**：

- ✅ 「标记已完成作答」按钮**确实渲染出来了**（count=1）
- ✅ 点击前「提交并查看结果」`disabled=True`
- ✅ 文案是「先选择至少一个答案，才能提交。」
- ❌ **点「标记已完成作答」→ 写请求数 = 0**（零网络活动，纯 no-op）
- ❌ 点击后按钮文案**没变**（`isTextAnswered` 永远是 undefined）
- ❌ 点击后提交按钮**仍 disabled**
- ❌ 没有出现「已作答 N 题」
- ✅ console error = 0 ⇒ **静默失败**，最难查的那种
- 因为 0 写请求，**没有产生任何测试数据**，无需清理

⇒ **结论：不能解决。** 断点还是同一个：`src/pages/PracticePage.tsx` **不在这次本地改动里**（提交前就没动过），
两个 props 没接、`answeredCount = savedByItem.size` 仍只数选择题 ⇒ 翻译大题永远提交不了 ⇒ 结果页出不来。
（另：`README.md` 自己已经把「主观题在线作答」列在 Roadmap 未实现里，与实测一致。）

## Goal 6.3 落地（用户拍板「改」）+ E2E 29/29（**未 commit、未 push**）

**方案：离线作答 + 在线标记** —— 用户在草稿纸上翻译，回页面点「标记已完成作答」，
写一条 `text_answer = ''`、`selected_option = null` 的纯标记行。**不采集任何在线文本输入**
⇒ 不触碰 §4「不包含主观题在线输入提交」的红线，所以 §4 不用改，6.3 就在 Phase 6 收口。

**代码改动：**

- `PracticePage.tsx`：接线两个 props、`answeredCount` 改逐题判定、`canSubmit` 分流、
  `toggleTextAnswer`（标记→upsert / 撤销→delete）、`submitHint()` 四分支文案
- `PracticeQuestion.tsx`：加 `canMark = typeof onToggleTextAnswer === 'function'` 守卫 ——
  **没回调就不渲染按钮**，避免再出现"看着能点、点了没反应"的死按钮
- `practice.ts` +`deletePracticeAnswer()`；`use-practice-mutations.ts` +`useDeletePracticeAnswer()`

**两个真 bug（都是 E2E 抓出来的，不是推演）：**

1. `answeredCount = savedByItem.size` —— 主观题写 `text_answer`、`selected_option` 恒 null，
   按选项集合计数 ⇒ 翻译大题（仅 1 小题）计数恒 0 ⇒ **提交按钮永远点不动**。
   改为「本题选过选项 **或** 标记过完成」。
2. `canSubmit = !hasChoiceItems` —— 把写作题也当成翻译题，渲染出一个点了没用的提交按钮
   外加假承诺「提交后可查看参考译文」。首轮 E2E **27/28** 就是这个；
   改为 `hasChoiceItems || canRevealReference`（写作题两者皆无 ⇒ 无提交入口）后 **29/29**。

**验证：**

- `tsc -b` exit 0；`pnpm lint` 7 warnings / 0 errors（全是既有的 `only-export-components`）；`pnpm build` exit 0（174 modules）
- E2E 29/29：标记→1 次 POST、撤销→1 次 **DELETE（真删行）**、重标→提交→参考译文渲染 327 字符、
  写作题提交按钮 count=0、选择题回归正确率 33%、作答阶段零泄漏、console error / pageerror 均 0
- 数据清理：dev 账号 5 个会话 + answers + mistakes 全删 ⇒ `remaining = 0 / 0 / 0`

**文档同步：** `docs/pages/practice.md`（§3 / 4.1 / 4.2 / 5 / 6 / 7 / 8 / 10 / 12 / 13）、
`docs/development/progress.md`、`docs/phases/phase-06-answer-review.md`（6.0–6.4 全部完成，Phase 6 可收口）。
顺手修正 phase-06 §9 的过时表述「先 migration，再 service」（本项目已无 migration 工作流）。

**状态：** `HEAD == origin/main == 9621c4e`；本轮改动全部留在工作区，**未 commit、未 push**。
