# 依赖
node_modules/
.pnpm-store/

# 构建产物
dist/
dist-ssr/
build/

# 环境变量（含 key，绝不提交）
.env
.env.local
.env.*.local

# 日志
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# TypeScript 增量构建信息
*.tsbuildinfo

# 编辑器 / 系统文件
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 注意：不要忽略 data/
# data/exams/*.json 是 GitHub 源题库，是本项目唯一的内容来源。

# Supabase CLI 临时状态（gen types 等命令会产生，属于本地状态，不提交）
supabase/.temp/

# 本地校验输出（tsc / oxlint / vite build 的 stdout 重定向产物）
# 不是项目文件，随时可重新生成：pnpm build / pnpm lint
.*-out.txt

# .workbuddy/ —— AI 协作的本地工作记忆与日志，属于本机产物，不入库
# ⚠️ 注意：.workbuddy/memory/* 在 2026-09-18 之前**已被跟踪并推送过**。
# 本规则只阻止**未来**的提交；要让 GitHub 上不再有它们，需要
# `git rm -r --cached .workbuddy` 后再提交推送。
# 另外：历史提交里仍然查得到（要彻底清除得用 filter-repo 改写历史，风险自担）。
.workbuddy/

